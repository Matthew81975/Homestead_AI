HCS-AI v0.10.0 — Models, Performance Telemetry, Prompt Functions, and Development Diagnostics

- Adds a dedicated Models tab with Hugging Face GGUF search, managed downloads,
  local import/list/delete/open-folder controls, active-model selection, and
  load/unload controls through the existing llama.cpp engine.
- Adds model compatibility metadata including file size, GGUF quantization,
  and a RAM-fit estimate.
- Records prompt-processing and generation tokens/second when the model server
  reports timings, with output-throughput fallback from token counts and wall time.
- Stores per-model performance history with a hardware signature for future
  automatic model routing.
- Shows the latest measured output and prompt throughput in the AI interface.
- Adds Prompt Functions: a restricted Python-like high-level language where
  prompts are inputs to ask()/ask_json() functions and ordinary control flow can
  compose LLM calls, HKR/KB queries, and HCS tools.
- Adds a Prompt Functions editor/library with JSON test inputs, basic syntax
  highlighting, execution traces, saved versions, and revision history.
- Prompt Function scripts are interpreted through a whitelisted AST subset;
  Python eval/exec is not used.
- Adds a top-level Log tab with severity/subsystem/search filters, copy controls,
  auto-scroll, and export to human-readable .log/.txt or structured .jsonl.
- Adds a runtime-toggleable Development Diagnostics layer. Normal mode stays
  compact; development mode expands the bottom status strip with backend port,
  HTTP state, active subsystem/operation, last error, and diagnostic-payload state.
- Adds a persistent bottom status strip showing the active model, last response
  time, and measured output throughput when available.
- HTTP failures now preserve and display useful backend response details instead
  of reducing failures to generic messages such as "HTTP Error 400: Bad Request".
- Knowledge Base imports now record chunking and classification stages and show
  the first concrete classification failure reason with a direct View Log action.
- Session diagnostics are written as rotating JSONL logs; approximately the ten
  most recent sessions are retained. Diagnostic payload capture is off by default.
